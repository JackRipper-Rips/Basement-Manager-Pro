<Window x:Class="SolusManifestApp.Views.Dialogs.PasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Password Required"
        Height="220"
        Width="450"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Border Background="{DynamicResource PrimaryDarkBrush}"
            CornerRadius="8"
            BorderBrush="{DynamicResource AccentBrush}"
            BorderThickness="2"
            Padding="20">
        <Border.Effect>
            <DropShadowEffect Color="Black" Opacity="0.5" BlurRadius="15" ShadowDepth="5"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,15">
                <TextBlock Text="🔒 Password Required"
                           FontSize="18"
                           FontWeight="Bold"
                           Foreground="{DynamicResource TextPrimaryBrush}"
                           VerticalAlignment="Center"/>
            </Grid>

            <!-- Content -->
            <StackPanel Grid.Row="1" Margin="0,0,0,15">
                <TextBlock Text="This archive is password protected. Please enter the password:"
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           TextWrapping="Wrap"
                           FontSize="13"
                           Margin="0,0,0,12"/>

                <Border Background="{DynamicResource SecondaryDarkBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="4">
                    <PasswordBox x:Name="PasswordBox"
                                 Padding="12,10"
                                 FontSize="14"
                                 Background="Transparent"
                                 Foreground="{DynamicResource TextPrimaryBrush}"
                                 BorderThickness="0"
                                 KeyDown="PasswordBox_KeyDown"
                                 VerticalContentAlignment="Center"/>
                </Border>

                <TextBlock Text="Press Enter to submit or Esc to cancel"
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           FontSize="11"
                           FontStyle="Italic"
                           Margin="0,8,0,0"
                           Opacity="0.7"/>
            </StackPanel>

            <!-- Buttons -->
            <StackPanel Grid.Row="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Content="OK"
                        Click="OkButton_Click"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Padding="35,10"
                        Margin="0,0,10,0"
                        MinWidth="90"/>
                <Button Content="Cancel"
                        Click="CancelButton_Click"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Padding="35,10"
                        MinWidth="90"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
